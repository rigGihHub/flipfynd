name: FlipFynd CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Apply compatibility patch
        run: python scripts/patch_public_import_progress_compat.py

      - name: Apply clean Seller Top 5 UI patch
        run: python scripts/patch_seller_top5_ui_clean.py

      - name: Apply dynamic Seller Top 5 patch
        run: python scripts/patch_seller_dynamic_top5.py

      - name: Apply Seller Top 5 resume resilience
        run: python scripts/patch_seller_resume_resilience.py

      - name: Apply Seller Top 5 incomplete-profile continue patch
        run: python scripts/patch_seller_continue_incomplete.py

      - name: Apply Seller Top 5 interrupted-profile resume guard
        run: python scripts/patch_seller_profile_resume_guard.py

      - name: Apply Seller Top 5 mobile UX cleanup
        run: python scripts/patch_seller_top5_mobile_ux.py

      - name: Apply Seller Top 5 v0.12.85 continuation fix
        run: python scripts/patch_seller_top5_v01285.py

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Compile Python sources
        run: python -m compileall -q app.py src tests

      - name: Run test suite
        run: pytest -q

      - name: Commit verified Seller Top 5 changes
        if: github.event_name == 'push'
        run: |
          if git diff --quiet -- app.py src/seller_top5_controller.py src/public_seller_inventory.py; then
            echo 'No Seller Top 5 changes needed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '4189824+github-actions[bot]@users.noreply.github.com'
          git add app.py src/seller_top5_controller.py src/public_seller_inventory.py
          git commit -m 'Fix Seller Top 5 continuation and first-batch ranking'
          git push origin HEAD:main
